# Complete-paper review

The current paper contains **five technical pages and a sixth page containing references**, following the user's removal of the page limit. The IEEEtran conference class, body font size, and margins are retained. Three figures, six tables, ten numbered equations, and the conclusion precede the references. The final build has no LaTeX warnings, unresolved references, or overfull boxes. Figure 2 remains within one column, and the added class-wise Table II spans both columns. Earlier page-count notes below describe prior revisions.

## Changes made

- Replaced the two proposition/proof blocks with numbered equations for the pruning objective, score perturbation, margin condition, and magnitude-based deletion rule. Added explicit equations for TM scoring, CPSS votes, signed supports, normalized attribution, and the scoped stability bound.
- Condensed the abstract, introduction, method, and results without reducing the body font or changing margins.
- Reconstructed full-pool CAS from cached activations and CPSS vectors. It reproduces report macro-F1 **0.765350 ± 0.029118**, against TM **0.692212 ± 0.004229**. Saved pruned-pool results remain **0.786807 ± 0.023303**. Both operating-point choices were checked against their selection-quarter grids.
- Corrected pruned-base SD rounding from 0.008 to **0.007** (actual 0.007466). Pruning reduces base macro-F1 by 0.004155; it does not cost literally nothing.
- Replaced the stale full-pool signature counts in Table III with counts matching the current B=15, threshold=0.8 vectors. Seed-42 positive supports are **42, 40, 32, 43, 28, 50**. Across all seeds the range is **21–101**, not 28–53.
- Recomputed the normalized sign-agnostic ablation on the report quarter: **0.025478 ± 0.031719**, not below 0.01 on every seed. The signed comparison uses identical vectors and settings.
- Clarified that the three-way experiment is retrospective. Earlier experiments inspected the original test data, and the scripts calculate report-quarter sweeps. The current selection rule uses selection-quarter scores, but the report quarter cannot be described as a previously untouched test read exactly once.
- Corrected the stability-bound interpretation. CPSS controls inclusions with low population selection probability under its sampling conditions. A false-discovery interpretation needs additional assumptions; estimated selection counts do not give a validated certificate, and the displayed expression is not finite at threshold 0.5. See [Shah and Samworth, Section 3.1](https://arxiv.org/pdf/1105.5578).
- Audited binary AUROC scoring. The reported **0.816580** baseline clips the TM attack class sum. Unclipped attack sums give **0.913167**; class-sum margins give **0.912339**. Table IV now includes both clipped and unclipped attack-score baselines, with CAS at **0.937871**.
- Verified all **62** nonempty synthetic unions of at most five signatures for each of the three current full-pool models. Clarified that this algebraic property of clause indices does not demonstrate real-traffic mixture recognition.
- Labeled earlier open-set and cross-seed studies as exploratory and separate from the current report split. Corrected the open-set numbers to the saved individual result files and explained why unmatched clause indices cannot establish semantic instability across trained models.
- Corrected bibliographic metadata for the [weighted TM paper](https://arxiv.org/abs/1911.12607), [SHAP/LIME forensic-analysis paper](https://www.mdpi.com/2076-3417/15/13/7329/notes), and [IoMT dataset-comparison paper](https://upcommons.upc.edu/entities/publication/6a1b8ea7-37d8-4377-97d6-7995081812e5). Reordered references by first citation. Dataset details were checked against the [CIC dataset description](https://www.unb.ca/cic/datasets/iomt-dataset-2024.html), and recent IoMT TM citations against their [CICIoMT](https://arxiv.org/abs/2604.03205) and [MedSec](https://arxiv.org/abs/2605.16707) records.

## Evidence and remaining limits

`results_audit.json` records the numeric checks and reconstructed metrics; `layout_audit.json` records page locations and compilation checks. The reproducible numeric audit is `../../scripts/audit_paper_results.py`. The pre-review source and PDF are preserved alongside this note.

This review did not retrain the models or rerun all CPSS fits. It reconstructed full-pool scoring and binary TM baselines from cached arrays, verified saved aggregates and selection rules, and checked the paper against these artifacts. The paper remains a single-dataset retrospective study with three training seeds. Independent evaluation, aligned semantic rule comparisons, and analyst-facing explanation validation remain outstanding research, now stated as limitations rather than established results.

## Restored results and methodology separation

Restored the matched-setting pruning comparison (0.777 ± 0.019 versus 0.765 ± 0.029), binary macro-F1 (0.629 ± 0.002 versus 0.823 ± 0.016), and synthetic noisy-mixture decoding with a new compact table. The synthetic experiment was rerun from the current saved full-pool CPSS vectors and exactly matches the earlier saved results for all seeds; details are in `restored_results_audit.json`. Methodology and Figure 1 now describe the general algorithm without dataset-specific settings or pruning outcomes. Numerical experiment settings are in Experimental Setup and selected operating points are in Results. All technical content still finishes on page 4; page 5 contains only references.

Figure 1 was further compacted to two rows of four boxes (7.16 × 1.70 inches before LaTeX scaling), with the detailed score formula removed. Figure 2 now overlays full-pool dashed curves and pruned-pool solid curves for both macro-F1 and accuracy in each sweep, retaining one-column placement. A short pruning explanation in Results is explicitly tentative; no causal mechanism has been established.

The Introduction and Related Work section now contains two paragraphs: attack-family attribution and neural-network classification leading to the interpretability gap, followed by TM prior work and the CAS motivation. Added Yin et al., IEEE Access 5, 21954–21961 (2017), DOI 10.1109/ACCESS.2017.2762418, checked against the published paper. Removed four uncited bibliography entries; all 12 retained references follow first-citation order. The final two-pass build retains four technical pages and a references-only fifth page without LaTeX warnings.

Expanded the two introduction paragraphs with the purpose of family attribution and compact, reusable rule evidence. Added audited full-pool per-class F1 gains and losses, overall accuracy, the relative evidence-list reduction, and binary Benign F1 and missed-attack trade-offs. `expanded_results_audit.json` records the derived quantities. The methodology is byte-for-byte unchanged from the source at the start of this expansion. A clean two-pass build keeps all technical content on four pages and references alone on page 5.

Rewrote Introduction and Related Work as exactly four paragraphs: attack-family attribution and neural-network interpretability, TM prior work, the compact-signature gap, and CAS contributions. Everything from the Method section onward is byte-for-byte unchanged in this revision. The clean two-pass build retains four technical pages and a references-only fifth page.

Removed the redundant full-versus-pruned Figure 4 from the manuscript and redirected its results reference to Figure 2(a). Kept the underlying numerical comparison. The rebuilt PDF has three figures, four technical pages, and a references-only fifth page, with no LaTeX warnings.

Expanded Discussion and Conclusion to three concise paragraphs using results already reported in the manuscript, covering pruning, class-specific and binary trade-offs, scope, limitations, and future evaluation. Rechecked all abstract numeric claims against saved experiment summaries, Table I, Table III, Table IV, and the results text; the values required no correction. Clarified CPSS as repeated half-sample selection and limited exact identifiability to tested synthetic unions. `abstract_conclusion_audit.json` records supporting values and locations. Introduction, methodology, and results are unchanged. The final build has four technical pages and references only on page 5, without LaTeX warnings.

Added class-wise Table II for the verified full-pool TM/CAS comparison: six classes, report-quarter support counts, and three-seed mean ± SD for precision, recall, and F1, followed by a macro-average row. Values come from the independently reconstructed per-seed classification reports in `results_audit.json`; the macro-F1 aggregates match Table I. `classwise_table_audit.json` records every table value and aggregation convention. Updated the results discussion to cite the table and report the precise Recon comparison. Removed the obsolete four-page constraint comment; the methodology remains byte-for-byte unchanged.

Added accuracy to Table I for all four methods, with mean ± SD over the same three seeds and report partition. Baseline values use the cached classification reports and verified pruned-baseline per-seed results; CAS values match `threeway_cas.json`. `table1_accuracy_audit.json` records all values. The table remains within one column and the final build has no warnings.

Rewrote Pruning and Signature Size around Figure 3 and post-pruning results: the removal curve, selected 390-clause pool, pruned TM and CAS macro-F1/accuracy from Table I, and pruned signature counts. Distinguished Figure 3's base-detector scores from the subsequent CAS attribution scores. Moved the matched full/pruned CAS comparison to Sensitivity Before and After Pruning. Updated the Figure 3 caption to name weighted-TM metrics. Methodology is unchanged, numeric claims match existing results, and the build has no warnings.

Corrected the signature-size table caption to “Signature sizes of the proposed CAS method before and after pruning,” with its common CPSS settings and seed. Replaced the accompanying discussion with one paragraph describing only the table's values: total selected supports decrease for all six classes, whereas positive support increases for DoS from 32 to 42 and decreases for the other five. Explicitly distinguished these seed-42 clause counts from three-seed averages. All claims match the saved grid supports, and the build has no warnings.
